function Post0To1(data) {
	const new_data = { v:db.manager.collection, a:[] }

	try {
		if (data.a.length > 0) {
			for (let i = 0, l = db.manager.collection; i < l; i++) {

			}
		}
	} catch(err) {
		console.error(err)
		Alert(err)
	}
}

function Collection0To1(data) {
	const new_data = { v:db.manager.collection, a:[] }

	try {
		if (data.a.length > 0) {
			for (let i = 0, l = db.manager.collection; i < l; i++) {

			}
		}
	} catch(err) {
		console.error(err)
		Alert(err)
	}
	
}